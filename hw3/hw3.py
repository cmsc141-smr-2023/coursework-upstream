import math

"""
CMSC 14100
Summer 2023

Homework #3

People Consulted:
   List anyone (other than the course staff) that you consulted about
   this assignment.

Online resources consulted:
   List the URL of any online resources other than the course text and
   the official Python language documentation that you used to complete
   this assignment.


[RESUBMISSIONS ONLY: Explain how you addressed the grader's comments
 for your original submission.  If you did not submit a solution for the
 initial deadline, please state that this submission is new.]
"""

def longest_string(strings):
    """
    Select the longest string from a list of strings.
    If the list is empty, return None.
    If there are multiple longest strings, choose any one of them.

    Inputs:
        strings, a list of strings

    Returns a string or None
    """
    return 'not implemented' # replace this with meaningful code


def exclaim(strings):
    """
    Add an exclamation point to every string in the list.
    Note: do not modify the existing list, build a new list.

    Inputs:
        strings, a list of strings

    Returns a list of strings
    """
    return 'not implemented' # replace this with meaningful code


def count_x(s):
    """
    Count all occurences of uppercase and lowercase x in the given string.

    Inputs:
        s, a string

    Returns an integer    
    """
    return 'not implemented' # replace this with meaningful code


def count_below(threshold, numbers):
    """
    Count all numbers strictly below the given threshold.

    Inputs:
        threshold, a float
        numbers, a list of floats
    
    Returns an integer
    """
    return 'not implemented' # replace this with meaningful code


def sum_above(threshold, numbers):
    """
    Compute the sum of all numbers strictly above the threshold.

    Inputs:
        threshold, a float
        numbers, a list of floats

    Returns a float
    """
    return 'not implemented' # replace this with meaningful code


def triangle_area(points):
    """
    Compute the area of the triangle.
    Assume the three given points are a well-formed triangle.

    Inputs:
        points: a 3-tuple of 2-tuples of floats

    Returns a float
    """
    return 'not implemented' # replace this with meaningful code


def farthest(o, points):
    """
    Return the point farthest from the point "o".
    Return None if list is empty.
    Return any farthest point in case of a tie.

    Inputs:
        o: a 2-tuple of floats
        points: a list of 2-tuples of floats

    Returns a 2-tuple of floats or None
    """
    return 'not implemented' # replace this with meaningful code


def inside(c, points):
    """
    Return the list of points strictly inside the circle.

    Inputs:
        c: a 2-tuple of floats (the center), and a float (the radius)
        points: a list of 2-tuples of floats

    Returns a list of 2-tuples of floats
    """
    return 'not implemented' # replace this with meaningful code


def bounding_box(points):
    """
    Return the smallest axis-aligned box containing all points.
    Return None if list is empty.
    
    Inputs:
        points: a list of 2-tuples of floats

    Returns a 2-tuple of 2-tuples of floats (upper left and
        lower right corners), or None
    """
    return 'not implemented' # replace this with meaningful code

